# Example secrets file for JolChobi
# Copy this to secrets.toml and add your actual keys

# OpenAI API key for LLM scenario notes
OPENAI_API_KEY = "sk-your-openai-api-key-here"

# Optional: Custom Overpass API endpoint
# OVERPASS_ENDPOINT = "https://your-custom-overpass-instance.com/api/interpreter"

# Optional: WMS service credentials (if needed)
# WMS_USERNAME = "your-username"
# WMS_PASSWORD = "your-password"
